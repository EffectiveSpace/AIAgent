# Упражнения к Lesson 6: Краткосрочная память и контекстное окно

## Упражнение 1: Анализ проблем краткосрочной памяти

### Цель
Понять ограничения и проблемы контекстного окна.

### Задание
1. Запустите агента с базовой краткосрочной памятью из урока
2. Проведите с ним диалог из 20-30 сообщений, в котором вы сначала сообщаете ему ключевую информацию (например, "Я люблю кофе с молоком", "Мой любимый цвет - синий", "Я работаю программистом"), а затем спрашиваете о чем-то, что требует вспомнить эту информацию
3. Увеличивайте диалог до тех пор, пока агент не "забудет" начальные факты

### Вопросы для анализа
1. После какого количества сообщений агент потерял контекст?
2. Какие типы информации теряются в первую очередь?
3. Как можно было бы улучшить сохранение критической информации?

### Дополнительно
Попробуйте повторить это с разными стратегиями управления памятью (sliding window vs pinned window).

## Упражнение 2: Реализация разных стратегий управления памятью

### Цель
Практически реализовать и сравнить разные стратегии управления краткосрочной памятью.

### Задание
Создайте три версии менеджера памяти:

### 1. Sliding Window (Скользящее окно)
```python
class SlidingWindowMemory:
    """
    Хранит только последние N сообщений, удаляя самые старые.
    """
    def __init__(self, max_messages=10):
        self.max_messages = max_messages
        self.history = []
    
    def add_message(self, role, content):
        # Реализуйте логику добавления и обрезки
        pass
    
    def get_history(self):
        return self.history
```

### 2. Pinned Window (Окно с закреплением)
```python
class PinnedWindowMemory:
    """
    Всегда сохраняет системный промпт и первое сообщение пользователя,
    а остальные обрезаются по принципу sliding window.
    """
    def __init__(self, system_prompt, max_messages=10):
        self.system_prompt = system_prompt
        self.first_user_message = None
        self.stored_first = False
        self.max_messages = max_messages - 2  # -2 для системного и первого сообщения
        self.history = []
    
    def add_message(self, role, content):
        # Реализуйте логику с закреплением первых сообщений
        pass
    
    def get_history(self):
        # Верните полную историю с закрепленными сообщениями
        history = [{"role": "system", "content": self.system_prompt}]
        if self.first_user_message:
            history.append({"role": "user", "content": self.first_user_message})
        history.extend(self.history)
        return history
```

### 3. Token-Based Window
```python
import tiktoken

class TokenBasedMemory:
    """
    Управляет памятью на основе количества токенов, а не сообщений.
    """
    def __init__(self, system_prompt, max_tokens=8000):  # gpt-3.5-turbo имеет окно 4096 токенов
        self.system_prompt = system_prompt
        self.max_tokens = max_tokens
        self.history = []
        self.encoder = tiktoken.encoding_for_model("gpt-3.5-turbo")
    
    def count_tokens(self, text):
        """Подсчитывает количество токенов в тексте."""
        return len(self.encoder.encode(text))
    
    def total_tokens_in_history(self):
        """Подсчитывает общее количество токенов во всей истории."""
        total = self.count_tokens(self.system_prompt)
        for msg in self.history:
            total += self.count_tokens(msg['content'])
        return total
    
    def add_message(self, role, content):
        # Реализуйте добавление с проверкой лимита токенов
        pass
    
    def get_history(self):
        return [{"role": "system", "content": self.system_prompt}] + self.history
```

### Задание
1. Реализуйте все три класса
2. Протестируйте каждый из них с одинаковым диалогом
3. Сравните результаты по:
   - Количеству сохраненных сообщений
   - Количеству токенов
   - Сохранению ключевой информации
   - Времени работы

## Упражнение 3: Суммаризация истории диалога

### Цель
Реализовать стратегию суммирования для управления краткосрочной памятью.

### Задание
Создайте класс, который:
1. Хранит текущую историю диалога
2. Периодически (например, каждые 10 сообщений) суммирует часть истории с помощью вызова LLM
3. Заменяет старые сообщения их суммаризацией
4. Сохраняет последние несколько сообщений "как есть" для контекста

```python
class SummarizingMemory:
    def __init__(self, system_prompt, summarization_interval=10):
        self.system_prompt = system_prompt
        self.interval = summarization_interval
        self.history = []
        self.summary = ""
        self.message_count = 0
        # Добавьте клиента OpenAI для вызова суммаризации
        # self.client = OpenAI(...)
    
    def add_message(self, role, content):
        # Добавьте сообщение
        # Проверьте, нужно ли выполнить суммаризацию
        pass
    
    def summarize_history(self):
        """Вызывает LLM для суммирования истории."""
        # Реализуйте вызов LLM для создания суммаризации
        pass
    
    def get_history(self):
        """Возвращает полную историю с учетом суммаризации."""
        # Если есть суммаризация, включите её в начало
        pass
```

### Вопросы для анализа
1. Как качество суммаризации влияет на дальнейшее взаимодействие?
2. Как часто нужно выполнять суммаризацию?
3. Какие типы диалогов лучше подходят для этой стратегии?

## Упражнение 4: Анализ стоимости и производительности

### Цель
Понять, как управление памятью влияет на стоимость и производительность.

### Задание
Создайте простой бенчмарк для сравнения разных стратегий управления памятью:

```python
import time
import random
import string

def generate_random_message(length=50):
    """Генерирует случайное сообщение заданной длины."""
    letters = string.ascii_lowercase + ' '
    return ''.join(random.choice(letters) for i in range(length))

def benchmark_memory_strategy(memory_instance, num_messages=20):
    """
    Измеряет время и приблизительную стоимость использования стратегии памяти.
    """
    start_time = time.time()
    
    for i in range(num_messages):
        role = "user" if i % 2 == 0 else "assistant"
        content = generate_random_message(100)  # 100 слов
        memory_instance.add_message(role, content)
        
        # Получаем историю (имитирует запрос к API)
        history = memory_instance.get_history()
    
    end_time = time.time()
    
    # Подсчитываем приблизительное количество токенов
    total_tokens = 0
    encoder = tiktoken.encoding_for_model("gpt-3.5-turbo")
    for msg in history:
        total_tokens += len(encoder.encode(msg['content']))
    
    print(f"Стратегия: {memory_instance.__class__.__name__}")
    print(f"Время выполнения: {end_time - start_time:.4f} сек.")
    print(f"Общее количество токенов: {total_tokens}")
    print(f"Количество сообщений в истории: {len(history)}")
    print("-" * 50)

# Протестируйте все реализованные стратегии
```

### Задание
1. Запустите бенчмарк для всех реализованных стратегий
2. Повторите тест с разным количеством сообщений (10, 50, 100)
3. Рассчитайте примерную стоимость для каждого сценария (используя тарифы OpenAI)

### Вопросы для анализа
1. Какая стратегия наиболее эффективна по соотношению токены/качество?
2. Какая стратегия самая быстрая?
3. Какие компромиссы вы бы выбрали для production-сценариев?

## Упражнение 5: Реализация кастомной стратегии управления памятью

### Цель
Создать собственную, более сложную стратегию управления памятью.

### Задание
Разработайте и реализуйте собственную стратегию, которая может комбинировать несколько подходов:

Возможные идеи:
- **Важность сообщений**: Определяйте важность сообщений (например, по длине, количеству ключевых слов) и сохраняйте наиболее важные
- **Контекстно-зависимое обрезание**: Сохраняйте сообщения, связанные с текущей темой разговора
- **Адаптивное управление**: Автоматически регулировать размер памяти на основе сложности задачи

```python
class AdaptiveMemory:
    """
    Адаптивная стратегия управления памятью.
    """
    def __init__(self, system_prompt, initial_max_messages=10):
        self.system_prompt = system_prompt
        self.max_messages = initial_max_messages
        self.history = []
        self.importance_scores = []  # Для хранения важности сообщений
    
    def calculate_importance(self, message):
        """Определяет важность сообщения (0.0 - 1.0)."""
        # Реализуйте логику оценки важности
        content = message['content'].lower()
        important_keywords = ['важно', 'ключевой', 'нужно', 'обязательно', 'критично']
        score = sum(1 for keyword in important_keywords if keyword in content)
        return min(score / len(important_keywords), 1.0) if important_keywords else 0.0
    
    def add_message(self, role, content):
        # Добавьте сообщение с оценкой важности
        # Реализуйте логику обрезки на основе важности
        pass
    
    def get_history(self):
        return [{"role": "system", "content": self.system_prompt}] + self.history
```

### Вопросы для анализа
1. Как вы определяете "важность" сообщения?
2. Насколько сложнее реализация по сравнению с базовыми стратегиями?
3. Какие преимущества и недостатки у вашей стратегии?

## Упражнение 6: Интеграция с реальным агентом

### Цель
Интегрировать реализованную систему памяти с полнофункциональным агентом.

### Задание
Возьмите одного из агентов, созданных в предыдущих уроках (например, агента-исследователя), и замените его простую историю сообщений на один из реализованных менеджеров памяти.

### Требования
1. Агент должен продолжать корректно работать с выбранной стратегией памяти
2. Должна сохраняться функциональность цикла ReAct
3. Должна отслеживаться и выводиться статистика по использованию памяти

### Проверка
Проведите тестовый диалог, включающий:
- Сложные многошаговые запросы
- Повторные вопросы о ранее упомянутой информации
- Длинные сессии с большим количеством сообщений

## Упражнение 7: Обработка ошибок и граничные случаи

### Цель
Обеспечить надежность системы памяти в граничных ситуациях.

### Задание
Добавьте обработку следующих ситуаций в ваши реализации:

1. **Пустая история**: Что происходит при попытке получить историю без сообщений?
2. **Очень длинные сообщения**: Как система реагирует на сообщения, превышающие размер контекста?
3. **Недопустимые роли**: Что происходит при попытке добавить сообщение с неизвестной ролью?
4. **Обрезка в середине темы**: Как агент справляется с разрывом контекста?

### Задание
```python
def test_edge_cases(memory_instance):
    """Тестирует граничные случаи."""
    
    # Тест 1: Пустая история
    print("Тест 1: Пустая история")
    history = memory_instance.get_history()
    print(f"История: {history}")
    print(f"Длина: {len(history)}")
    
    # Тест 2: Очень длинное сообщение
    print("\nТест 2: Очень длинное сообщение")
    long_msg = "Это очень длинное сообщение. " * 1000
    memory_instance.add_message("user", long_msg)
    print("Сообщение добавлено")
    
    # Другие тесты...
```

### Вопросы для анализа
1. Какие ошибки возникли?
2. Как вы их исправили?
3. Какие дополнительные проверки стоит добавить?

## Упражнение 8: Сравнение с фреймворками

### Цель
Сравнить собственные реализации с системами памяти в популярных фреймворках.

### Задание
Изучите реализацию памяти в LangChain:

```python
from langchain.memory import ConversationBufferMemory, ConversationTokenBufferMemory
from langchain.chains import ConversationChain
from langchain_openai import ChatOpenAI

# Пример использования встроенной памяти LangChain
llm = ChatOpenAI(model="gpt-3.5-turbo")
memory = ConversationTokenBufferMemory(llm=llm, max_token_limit=1000)

conversation = ConversationChain(
    llm=llm,
    memory=memory,
    verbose=True
)

# Протестируйте и сравните с вашей реализацией
```

### Вопросы для анализа
1. Чем реализации в фреймворках отличаются от ваших?
2. Какие дополнительные возможности они предоставляют?
3. В каких случаях лучше использовать встроенные решения, а когда - свои?

---

## Дополнительные задания для продвинутых

### Задание 9: Реализация памяти с сохранением на диск
Создайте систему памяти, которая может сохранять и восстанавливать историю между сессиями.

### Задание 10: Память с классификацией тем
Реализуйте систему, которая может классифицировать сообщения по темам и предоставлять доступ к информации по определенным темам.

---

После выполнения упражнений сравните эффективность разных подходов к управлению краткосрочной памятью и подумайте, как бы вы применили эти знания в реальных проектах с ИИ-агентами.